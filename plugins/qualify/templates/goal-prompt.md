# goal 提示词模板(定量渲染的目标产物)

> 定量层读定性填空表 + 项目档案(流程骨架) + 规矩库注入,渲染出这份 goal 提示词。
> 段 1/2/3 来自定性填空表(变化),段 5/6/7/8 来自项目档案(固定)。
> ≤ 4000 字符。详细规则放 tasks.json,提示词只放调度逻辑。

---

把 {项目名} 的 Phase {X}({槽1 What})落地,目标:{槽5 完成条件的关键指标}。

【保护铁律】{槽2 Scope 的只读部分}。所有命令只在 {可写范围} 下运行。

【完成条件】
1. {槽5 完成条件 1}
2. {槽5 完成条件 2}
...
{+ build/test/文件行数等通用条件}

【工作目录】{项目根绝对路径}

【可写范围】{槽2 Scope 的可写部分}

【只读参考】{槽2 Scope 的只读部分 + 规矩库注入的相关规矩}

【调度算法】
{抄自 project.profile.md 的流程骨架}

【派子代理 prompt 必须包含】
{抄自 project.profile.md}

【每轮报告(≤ 300 字)】
{抄自 project.profile.md}

【状态机】pending→in_progress→completed;needs_human/failed/blocked。

【关键约束】{规矩库注入的相关规矩,逐条列出}

【绝对禁止】
{槽2 的只读禁改 + 项目级 forbidden_actions}

【遇到死循环】立即停止,向用户报告当前状态。
