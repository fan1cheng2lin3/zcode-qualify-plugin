# 评审报告模板

> 复制这份模板,改名 `docs/reviews/phase-{X}-review.md`,填完即一份评审产物。
> 评审是定量 agent 下一轮的输入——格式必须固定,这样定量能可靠提取"哪里没通过"。
>
> 评审读的是 **git diff + grep/build/test 结果**,不是几小时的对话历史。

---

# Phase {X} 评审报告

## 元信息

- **评审时间**:{日期}
- **评审对象**:Phase {X}(对应定性表 `docs/phases/phase-{X}.md` + 定量产物 `.qualify/{phase}/`)
- **评审依据**:定性表槽 5(完成条件)逐条核对
- **git diff 摘要**:{改了哪些文件,几行} <!-- 工具自动填:git diff --stat -->

---

## 结论:{✅ 通过 / ❌ 未通过 / ⚠️ 部分通过}

<!-- 一句话说清整体情况。未通过时必须指明回定量还是回执行。-->

---

## 完成条件逐条核对(对照定性表槽 5)

| # | 完成条件(摘自定性表) | 结果 | 证据 |
|---|---|---|---|
| 1 | {条件原文} | ✅/❌ | {grep/build/test 的实际输出摘要} |
| 2 | {条件原文} | ✅/❌ | {证据} |

---

## 执行摘要(工具自动跑,不用大模型)

<!-- 这段由调度脚本自动生成,不是 LLM 写的。零 token。 -->

### git diff
```
{git diff --stat 输出}
```

### 关键检查
- **build**:{命令} → {结果}
- **test**:{命令} → {N 通过 / N 失败}
- **grep 指纹**:{pattern} → {命中数}
- **保护边界**:{禁改目录} → {零修改 / 有修改}

---

## 没做到位的(❌ 或 ⚠️ 的详情)

<!-- 逐条列出未通过的完成条件,说明:
- 期望是什么(定性表怎么要求的)
- 实际是什么(代码/测试的真实状态)
- 差距在哪
- 建议怎么修(给定量 agent 的提示)
-->

### 未通过项 1
- **期望**:{...}
- **实际**:{...}
- **差距**:{...}
- **建议修法**:{给定量 agent 下一轮的提示}

---

## 下一轮指令(给定量 agent)

<!-- 如果结论是"未通过",这里写清楚下一轮定量该干什么。
定量 agent 读这段 + 上面的"没做到位",生成新的 tasks.json + goal.md。 -->

{如果通过:无}
{如果未通过:针对上述未通过项,生成下一轮任务,重点解决 XXX}
